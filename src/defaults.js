/**
 * Define defaults to use as backup values. These are used in replacements.js
 */
const defaults = {
  meta: {
    description: 'Henlo, fren',
  },
  title: 'Chad Lavimoniere',
}

module.exports = defaults
